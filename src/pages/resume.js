import React from 'react'
import PropTypes from 'prop-types'

const Resume = () => {
  return <page className={`page resume`}>
      <h1 className="page-title">Resume</h1>
    </page>
}

Resume.propTypes = {

}

export default Resume
