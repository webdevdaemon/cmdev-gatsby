import React from 'react'
import PropTypes from 'prop-types'

const PostInfoBar = () => {
  return (
    <div className={`post-info-bar`} >
      
    </div>
  )
}

PostInfoBar.propTypes = {
  
}

export default PostInfoBar
